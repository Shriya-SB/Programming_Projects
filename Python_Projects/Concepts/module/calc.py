# Modules are the in-built functions in the python to execute the code.
# There are two types of module external modules and internal modules
# We can even create new modules and execute the code.
# For example:
import math_opr  # Import the module created
# Pass the parameter and use according your need.
print(math_opr.subract(40, 5))
print(math_opr.divide(40, 2))